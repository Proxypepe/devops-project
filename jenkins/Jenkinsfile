pipeline {
    agent none
    stages {
        stage("Test configurations") {
            agent slave
            steps {
                sh 'ansible-lint ansible/'
            }
        }

        stage("Working with code") {
            agent slave
            steps {
                sh 'flake8 src/'
            }            
        }
    }
}
